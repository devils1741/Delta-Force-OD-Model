model:
  onnx_path: weights/best.onnx
  engine_path: weights/best.engine
  input_width: 960
  input_height: 960

inference:
  target_fps: 120
  score_threshold: 0.30
  nms_threshold: 0.45
  max_detections: 300

capture:
  output_index: 0
  roi_width: 1600
  roi_height: 900

tensorrt:
  fp16: true
  workspace_mb: 1024
